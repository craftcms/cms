#!/usr/bin/env bash
set -euo pipefail

# Tools, dependencies, and generated assets are restored from the snapshot.
# SQLite needs no service restart; do not install or rebuild anything on wake.
if [[ -n ${CRAFT_FONTAWESOME_TOKEN:-} ]]; then
    pnpm config set '//npm.fontawesome.com/:_authToken' "$CRAFT_FONTAWESOME_TOKEN"
fi
