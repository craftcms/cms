# `craft-nav-item` accessibility

## Requirements

- [x] A group heading keeps its label aligned with sibling headings even when a nested item provides a slotted icon, without removing that item's accessible label. Verified by `nav-item.browser.test.ts`.
