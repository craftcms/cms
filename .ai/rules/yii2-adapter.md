---
paths:
  - 'yii2-adapter/**'
---

# Yii2 Adapter

## Keep the adapter compatibility-only
Put plugin compatibility shims, legacy constants, event bridges, and Yii fallbacks here. Follow the adapter Composer autoload mapping; reserve `yii2-adapter/lib` for vendored or library-style code.
