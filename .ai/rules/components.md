---
paths:
  - 'src/Cp/Components/**'
---

# Components

## Build server-rendered UI as registered components
Extend `ViewComponent`, expose typed fluent setters, and register components in `ComponentRegistry`. Preserve the `Component::make()->configure()` configuration path.
