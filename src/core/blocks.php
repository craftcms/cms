<?php

/**
 * Blocks Class
 */
//class Blocks {

//	var $globals = array();

	/**
	 * Blocks Constructor
	 */
/*	function __construct()
	{
		// make this global
		global $B;
		$B = $this;

		// load the Config class
		require_once CORE_PATH.'site.php';
		$this->config = new Config();

		// load the URI class
		require_once CORE_PATH.'uri.php';
		$this->uri = new URI();

		// create the URI segment globals
		foreach ($this->uri->segments as $i => $segment) {
			$this->globals['segment_'.($i+1)] = $segment;
		}

		// load the DB class
		require_once CORE_PATH.'db.php';
		$this->db = new DB();

		$this->globals['site_url'] = $this->config->item('site_url');
	}

}
*/
