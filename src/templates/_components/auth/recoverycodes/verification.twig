{% import '_includes/forms.twig' as forms %}

{{ forms.hidden({
    'name': 'currentMethod',
    'value': currentMethod
}) }}

{% for field in fields %}
    {{ forms.textField({
        'label': field.label,
        'id': field.key,
        'name': field.namespacedKey,
        'placeholder': 'XXXXXX'
    }) }}
{% endfor %}

{{ forms.submitButton({
    id: 'auth2fa-verify',
    class: 'auth2fa-verify',
    label: 'Verify'|t('app'),
    spinner: craft.app.request.isCpRequest(),
}) }}