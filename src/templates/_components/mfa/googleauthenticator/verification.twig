{% import '_includes/forms.twig' as forms %}

{{ forms.hidden({
    'name': 'currentMethod',
    'value': currentMethod
}) }}

{% for field in fields %}
    {{ forms.textField({
        'label': field.label,
        'id': field.key,
        'name': field.namespacedKey,
        'placeholder': 'XXXXXX'
    }) }}
{% endfor %}

{{ forms.submitButton({
    id: 'mfa-verify',
    class: 'mfa-verify',
    label: 'Verify'|t('app'),
    spinner: true,
}) }}