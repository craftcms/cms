{% import '_includes/forms.twig' as forms %}

{% for key,label in fields %}
    {{ forms.textField({
        'label': label,
        'id': key,
        'name': key,
        'placeholder': 'XXXXXX'
    }) }}

    {{ forms.submitButton({
        id: 'mfa-verify',
        class: 'mfa-verify',
        label: 'Verify'|t('app'),
        spinner: true,
    }) }}
{% endfor %}