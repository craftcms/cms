<script type="text/javascript">
    var widget = window.parent.Craft.CraftSupportWidget.widgets[{{ widgetId }}];
    var response = {
        success: {{ success == true ? 1 : 0 }},
        errors: {{ errors|json_encode|raw }}
    };
    widget.currentScreen.parseSupportResponse(response);
</script>
