{% extends "settings/assets/_layout" %}
{% set selectedNavItem = 'filesystems' %}

{% do view.registerAssetBundle('craft\\web\\assets\\admintable\\AdminTableAsset') -%}

{% do view.registerTranslations('app', [
    "Name",
    "Handle",
    "Type",
    "No filesystems exist yet."
]) %}

{% block content %}
    <div id="filesystems-vue-admin-table"></div>

    <div class="buttons">
        <a class="btn submit add icon" href="{{ url('settings/assets/filesystems/new') }}">{{ "New filesystem"|t('app') }}</a>
    </div>
{% endblock %}

{% set tableData = [] %}
{% for filesystem in filesystems %}
    {% set filesystemIsMissing = false %}

    {% if filesystem is missing %}
        {% set filesystemIsMissing = true %}
    {% endif %}

    {% set tableData = tableData|merge([{
        id: filesystem.id,
        title: filesystem.name|t('site'),
        url: url('settings/assets/filesystems/' ~ filesystem.id),
        name: filesystem.name|t('site')|e,
        handle: filesystem.handle,
        type: {
            isMissing: filesystemIsMissing,
            label: filesystemIsMissing ? filesystem.expectedType : filesystem.displayName()
        },
    }]) %}
{% endfor %}

{% js %}
var columns = [
    { name: '__slot:title', title: Craft.t('app', 'Name') },
    { name: '__slot:handle', title: Craft.t('app', 'Handle') },
    { name: 'type', title: Craft.t('app', 'Type'), callback: function(value) {
            if (value.isMissing) {
                return '<span class="error">' + value.label + '</span>'
            }

            return value.label
        }
    },
];

new Craft.VueAdminTable({
    columns: columns,
    container: '#filesystems-vue-admin-table',
    deleteAction: 'filesystems/delete-filesystem',
    emptyMessage: Craft.t('app', 'No filesystems exist yet.'),
    tableData: {{ tableData|json_encode|raw }}
});
{% endjs %}
