{% apply spaceless %}

  {% for element in elements %}
    {% tag 'li' with {
      class: element.id in disabledElementIds ? 'disabled' : null,
      data: {
        id: element.id,
      },
    } %}
      {{ elementChip(element, {
        context: context ?? 'index',
        size: 'large',
        checkbox: showCheckboxes,
      }) }}
    {% endtag %}
  {% endfor %}

{% endapply -%}
