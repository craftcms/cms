{% set id = id ?? "languagemenu#{random()}" %}
{% set value = value ?? null -%}
{% set options = options ?? [] %}


{% if includeEnvVars ?? false %}
    {% set options = options|merge(craft.cp.getLanguageEnvOptions()) %}
{% endif %}

{% include '_includes/forms/selectize'  with {
    includeEnvVars: false,
    value: value,
}%}
