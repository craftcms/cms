{# Renders a <craft-input-password> web component from the legacy password
   variables — a text input with a built-in show/hide reveal toggle. This
   replaces the old `.passwordwrapper` + `<input type="password">` markup and the
   separate `Craft.PasswordInput` JS, which the component now handles natively.

   The legacy-to-component mapping lives in FormFields::passwordFromConfig(),
   which reuses the text-input mapping (see `_includes/forms/text`). #}

{%- set inputAttributes = inputAttributes ?? {} %}
{%- if block('attr') is defined %}
  {%- set inputAttributes = inputAttributes|merge(('<div ' ~ block('attr') ~ '>')|parseAttr, recursive=true) %}
{% endif %}

{{- craft.cp.password(_context)|raw -}}
