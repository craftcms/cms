{% import '_includes/forms.twig' as forms %}
{{ hiddenInput('userId', user.id) }}

{% if showUserGroups %}
  <fieldset class="first">
    <h2>{{ 'User Groups'|t('app') }}</h2>

    {% do DeltaRegistry.registerName('groups') %}

    {% embed '_includes/forms/field.twig' with {
      fieldId: 'user-groups',
      data: {'error-key': 'groups'},
    } %}
      {% block input %}
        {% include '_includes/forms/componentSelect.twig' with {
          name: 'groups[]',
          options: UserGroups.getAssignableGroups(user),
          values: user.getGroups(),
          showDescription: true,
          sortable: false,
          createAction: currentUserIsAdmin and config('craft.general.allowAdminChanges') ? 'user-settings/edit-group' : null,
        } %}
      {% endblock %}
    {% endembed %}
  </fieldset>
{% endif %}

{% if showPermissions %}
  {% requireEdition CraftTeam %}

  {% if showUserGroups %}
    <hr>
  {% endif %}

  <div>
    <h2>{{ 'Permissions'|t('app') }}</h2>

    {% if currentUserIsAdmin %}
      {{ forms.lightswitchField({
        label: 'Admin'|t('app'),
        name: 'admin',
        id: 'admin',
        on: user.admin,
        reverseToggle: 'permissions'
      }) }}
    {% endif %}

    {% do DeltaRegistry.registerName('permissions') %}
    {{ hiddenInput('permissions', '') }}

    <div id="permissions" class="field{% if user.admin %} hidden{% endif %}">
      {% if CraftEdition == CraftTeam %}
        <div class="readable mb-xl">
          <blockquote class="note tip">
            <p>
              {% if config('craft.general.allowAdminChanges') %}
                {{ 'Team permissions can be managed from {link}.'|t('app', {
                  link: tag('strong', {
                    html: tag('a', {
                      href: url('settings/users'),
                      text: 'User Permissions'|t('app'),
                    }),
                  }),
                })|raw }}
              {% else %}
                {{ 'Team permissions can be managed from {path} on a development environment.'|t('app', {
                  path: ['Settings'|t('app'), 'Users'|t('app')]
                    |map(p => tag('strong', {
                      class: 'nowrap',
                      text: p,
                    }))
                    |join(' → '),
                })|raw }}
              {% endif %}
            </p>
          </blockquote>
        </div>
      {% endif %}

      {% include '_includes/permissions' with {
        subject: user.admin ? null : user,
        permissions: UserPermissions.getAssignablePermissions(user),
        groupPermissions: user.id ? UserPermissions.getGroupPermissionsByUserId(user.id) : [],
        disabled: CraftEdition == CraftTeam,
      } only %}
    </div>
  </div>
{% endif %}

{% js %}
new Craft.ElevatedSessionForm('#main-form', [
  '[name="admin"][value=""]',
  '#user-groups input',
  '#permissions input[type="checkbox"]:not(:checked)',
]);
{% endjs %}
