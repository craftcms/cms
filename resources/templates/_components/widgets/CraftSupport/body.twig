 <support-widget
  id="{{ widget.id }}" bundle-url="{{ bundleUrl }}"
  issue-title-prefix="{{ issueTitlePrefix }}"
  issue-params="{{ issueParams | json_encode | e('html_attr') }}"
></support-widget>
