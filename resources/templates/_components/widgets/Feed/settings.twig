{% import "_includes/forms" as forms %}


{{ forms.textField({
    label: "URL"|t('app'),
    id: 'url',
    name: 'url',
    value: widget.url,
    required: true,
    errors: widget.errors.get('url')
}) }}


{{ forms.textField({
    label: "Title"|t('app'),
    id: 'title',
    name: 'title',
    value: widget.title,
    required: true,
    errors: widget.errors.get('title')
}) }}


{{ forms.textField({
    label: "Limit"|t('app'),
    id: 'limit',
    name: 'limit',
    value: widget.limit,
    size: 2,
    errors: widget.errors.get('limit')
}) }}
